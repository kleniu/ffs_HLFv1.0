export PATH=../000-getbinaries/bin/:$PATH
cat README.md | awk '
BEGIN{go=true}
{
	if($1=="```") 
		go=!go; 
	if(go && $1!="```") 
		print $0
}' | while read COMMAND; do
	echo "executing: $COMMAND"
	bash -c "$COMMAND"
done
